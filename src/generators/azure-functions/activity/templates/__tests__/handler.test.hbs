/* tslint:disable: no-any */
/* tslint:disable: no-big-function */

import { {{ FunctionName }}Handler } from "../handler";

jest.useFakeTimers('modern');

describe("{{ FunctionName }}Handler", () => {
  it("should return true", async () => {
    expect(true).toBe(false);
  });
});
